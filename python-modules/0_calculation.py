#!/usr/bin/python3
if __name__ == "__main__":
    # imports
    from calculation import add, sub, mul, div

    a = 4  # passing parameter one to function
    b = 2  # passing parameter two to function

    # Ading Print Statement with parameters.
    print(add(a, b))
    print(sub(a, b))
    print(mul(a, b))
    print(div(a, b))
